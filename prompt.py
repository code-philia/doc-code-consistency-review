ALIGN_PROMPT_TEMPLATE = """你是一位精通航天领域软件系统和C/C++编程的资深专家。
# 任务
给定一段Markdown格式的需求，以及一个带行号的代码文件的内容，请你帮我找出与该需求最相关的代码段。严格按照以下JSON格式返回结果：
```json
{{
  "related_code": [[start1, end1], [start2, end2], ...],
}}
```
# 提示
1.直接返回结果，不要输出其他思考和说明内容。例如，如果代码中的第5行到第10行，和第20到30行与需求相关，则返回：
```json
{{
  "related_code": [[5, 10], [20, 30], ...],
}}
```
如果没有相关的代码，则返回：
```json
{{
  "related_code": []
}}
```
2.相关指的是代码实现了需求中描述的功能或逻辑。如果需求中有表格或公式，请查找与之相关的代码行号。如果需求中有多个相关代码段，请全部返回，不要遗漏。

# 输入
## 需求如下：
{req_content}
\n## 代码如下：
{code_content}
"""


REVIEW_PROMPT_TEMPLATE = """
# 任务说明
作为一名航天软件质量审查专家，请对给定的需求文档片段和相关代码片段进行一致性审查。

# 输入
## 需求片段
{requirement}

## 相关代码片段
{related_code}

# 审查要求
1.  **审查分析过程**: 详细分析代码实现是否与需求描述一致。请关注功能、算法、参数、变量、安全等方面。分析过程应清晰、具体，并引用相关的代码和需求内容。
2.  **生成问题单**: 如果发现任何不一致之处，请生成一个问题单。如果代码完全符合需求，则问题单部分为 null。

# 输出格式
请严格按照以下JSON格式返回结果，不要添加任何额外的解释或说明。

```json
{{
  "review_process": "这里是详细的审查分析过程...",
  "issue": {{
    "level": "high" | "medium" | "low",
    "summary": "这里是对问题的简短总结",
    "description": "在[文件名]的[函数名/行号]处，程序实现是[具体代码实现]，而需求是[需求描述]，实现与需求不一致，原因是[技术分析]。"
  }}
}}
```

**示例**:
- 如果没有问题:
```json
{{
  "review_process": "代码功能与需求文档完全一致。factorial函数正确处理了n=0, n=1以及负数的情况，循环逻辑正确，返回类型也与需求匹配。",
  "issue": null
}}
```
- 如果有问题:
```json
{{
  "review_process": "在审查过程中发现，当输入为负数时，需求文档要求返回0，但代码实现返回了1，这与需求不符。",
  "issue": {{
    "level": "medium",
    "summary": "factorial函数对负数输入的处理与需求不一致",
    "description": "在 'algorithms/math_algorithms.cpp' 的 'factorial' 函数处，程序实现是 `if (n < 0) return 1;`，而需求是 `当n为负数时返回0`，实现与需求不一致，原因是代码错误地为负数输入返回了1。"
  }}
}}
```
"""


GENERATE_PROMPT_TEMPLATE = """
# 任务说明
你是一位精通航天领域软件系统和C/C++编程的资深专家。请你根据以下代码，生成描述该代码功能的需求片段，要求如下：
1. 生成的需求片段应简洁明了，能够清晰描述代码的功能和目的
2. 需求片段应包含必要的细节，但不需要过于冗长

# 相关代码
{related_code}

# 输出要求
直接输出生成的需求片段，不要输出其他内容。
"""