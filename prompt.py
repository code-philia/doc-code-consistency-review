ALIGN_PROMPT_TEMPLATE_ICL = """你是一位精通航天领域软件系统和C/C++编程的资深专家。
# 任务
给定一段Markdown格式的需求，以及一个包含若干个字典元素的代码块列表(列表中每个字典元素的格式为"文件名:..., 起止行号:..., 代码内容:...")，请你帮我在列表各个元素的**代码内容**中检索出与该需求**相关**的代码内容，返回一个列表，每个列表项是一个代码块，仅仅包括**文件名**和**行号范围**，不要包含代码内容，严格按照以下JSON格式返回结果：
```json
[{{"file":文件名, "range":[开始行号,结束行号], "similarity":相似度 }}, ...]
```
# 提示
1.返回一个列表，即使仅包含一个元素，尽量不返回空。
2.列表项中仅仅包含文件名、行号范围、相似度即可。相似度指的需求与该代码片段之间的相关性，数值是范围在[0,1]的两位小数，相似度需要具有区分性。
3.不要输出其他思考和说明内容。
4.相关指的是代码实现了需求中描述的功能或逻辑，注意查找最相关的代码，如果没有完全对应的代码，可以返回空列表。
5.你会得到一段参考的示例，示例中包含了匹配的需求和代码片段。


# 输入
## 需求如下：
```
{req_content}
```
\n## 代码块列表如下：
{code_content}
\n## 参考示例如下：
**示例需求（来自历史标注）**：
```
{icl_query_text}
```
**示例实现（来自历史标注）**
```
{icl_code_text}
```
/think
"""

ALIGN_PROMPT_TEMPLATE = """你是一位精通航天领域软件系统和C/C++编程的资深专家。
# 任务
给定一段Markdown格式的需求，以及一个包含若干个字典元素的代码块列表(列表中每个字典元素的格式为"文件名:..., 起止行号:..., 代码内容:...")，请你帮我在列表各个元素的**代码内容**中检索出与该需求**相关**的代码内容，返回一个列表，每个列表项是一个代码块，仅仅包括**文件名**和**行号范围**，不要包含代码内容，严格按照以下JSON格式返回结果：
```json
[{{"file":文件名, "range":[开始行号,结束行号], "similarity":相似度 }}, ...]
```
# 提示
1.返回一个列表，即使仅包含一个元素，尽量不返回空。
2.列表项中仅仅包含文件名、行号范围、相似度即可。相似度指的需求与该代码片段之间的相关性，数值是范围在[0,1]的两位小数，相似度需要具有区分性。
3.不要输出其他思考和说明内容。
4.相关指的是代码实现了需求中描述的功能或逻辑，注意查找最相关的代码，如果没有完全对应的代码，可以返回空列表。


# 输入
## 需求如下：
```
{req_content}
```
\n## 代码块列表如下：
```
{code_content}
```
/think
"""

ALIGN_REQ_PROMPT_TEMPLATE = """你是一位精通航天领域软件系统和C/C++编程的资深专家。
# 任务
给定一段代码块，以及一个包含若干个字典元素的需求块列表(列表中每个字典元素的格式为"文档名:..., 需求内容:...")，请你帮我在列表各个元素的**需求内容**中检索出与该代码**相关**的需求内容，返回一个列表，每个列表项是一个需求块，仅仅包括**文档名**、**需求内容**和**相似度**，严格按照以下JSON格式返回结果：
```json
[{{"filename":文档名, "content":需求内容, "similarity":相似度 }}, ...]
```
# 提示
1.返回一个列表，即使仅包含一个元素，尽量不返回空。
2.列表项中仅仅包含文档名、需求内容、相似度即可。相似度指的代码与该需求片段之间的相关性，数值是范围在[0,1]的两位小数，相似度需要具有区分性。
3.不要输出其他思考和说明内容。
4.相关指的是代码实现了需求中描述的功能或逻辑，注意查找最相关的需求，如果没有完全对应的需求，可以返回空列表。


# 输入
## 代码如下：
```
{code_content}
```
\n## 需求块列表如下：
```
{req_content}
```
/think
"""


REVIEW_PROMPT_TEMPLATE = """
# 任务说明
作为一名航天软件质量审查专家，请对给定的需求文档片段和相关代码片段审查，具体如下：
1.  分析输入的需求片段和代码片段是否相关，给定的若干代码片段有些相关、有些不相关，对于不相关的代码给出提示；对于相关的代码考虑后续问题。
2.  **生成审查分析过程**: 
（1）详细分析需求与代码实现的一致性，尽可能具体；
（2）请关注代码功能、算法、参数、变量、安全、逻辑等方面，关注以下方面：
- 功能一致性：代码是否实现需求描述的功能
- 算法一致性：实现算法是否与需求描述一致，是否正确实现了需求中的公式逻辑，注意运算符号和数值
- 参数一致性：常量/变量值是否符合需求，注意符号和精确度，以及数值
- 变量值一致性：变量的初始值和范围是否符合需求
- 潜在的风险和漏洞
（3）分析过程应详细、清晰、具体、全面，并引用相关的代码和需求内容。
3.  **生成问题单**: 如果发现任何不一致之处，请生成一个问题单`issue`。如果代码完全符合需求，则问题单部分为 null。

# 输出格式
请严格按照以下JSON格式返回结果，不要添加任何额外的解释或说明。

```json
{{
  "review_process": "这里是详细的审查分析过程...",
  "issue": {{
    "level": "high" | "medium" | "low",
    "summary": "这里是对问题的简短总结",
    "description": "在[文件名]的[函数名/行号]处，程序实现是[具体代码实现]，而需求是[需求描述]，实现与需求不一致，原因是[技术分析]。"
  }}
}}
```

**示例**:
- 如果没有问题:
```json
{{
  "review_process": "代码功能与需求文档完全一致。",
  "issue": null
}}
```
- 如果有问题:
```json
{{
  "review_process": "在审查过程中发现，当输入为负数时，需求文档要求返回0，但代码实现返回了1，这与需求不符。",
  "issue": {{
    "level": "medium",
    "summary": "factorial函数对负数输入的处理与需求不一致",
    "description": "在 'algorithms/math_algorithms.cpp' 的 'factorial' 函数处，程序实现是 `if (n < 0) return 1;`，而需求是 `当n为负数时返回0`，实现与需求不一致，原因是代码错误地为负数输入返回了1。"
  }}
}}
```
**示例结束**

# 输入
## 需求片段
```
{requirement}
```

## 相关代码片段
```
{related_code}
```
/think
"""


THINKING_PROMPT_TEMPLATE = """
# 任务说明
作为一名航天软件质量审查专家，请对给定的需求文档片段和相关代码片段审查，具体如下：
1.  **生成审查分析过程**: 
（1）分析输入的需求片段和代码片段是否相关，详细分析需求与代码实现的一致性，尽可能具体；
（2）请关注代码功能、算法、参数、变量、安全、逻辑等方面，关注以下方面：
- 功能一致性：代码是否实现需求描述的功能
- 算法一致性：实现算法是否与需求描述一致，是否正确实现了需求中的公式逻辑，注意运算符号和数值
- 参数一致性：常量/变量值是否符合需求，注意符号和精确度，以及数值
- 变量值一致性：变量的初始值和范围是否符合需求
- 边界范围一致性：分段函数的定义域区间是否正确，注意开区间和闭区间，例如Q<100不能写成Q<=100
- 潜在的风险和漏洞
（3）分析过程应详细、清晰、具体、全面，并引用相关的代码和需求内容。
2.  **生成问题单**: 如果发现任何不一致之处、以及代码存在功能、算法、参数、变量、安全、逻辑等问题，请注意**针对每个问题分别生成问题单**也就是生成`issue`列表。如果代码完全符合需求，则问题单部分为 null。

# 输出格式
请严格按照以下JSON格式返回结果，不要添加任何额外的解释或说明。

```json
{{
  "review_process": "这里是详细的思考过程，分多个步骤，说清楚你分析和推导的逻辑...",
  "issue": [{{
    "level": "high" | "medium" | "low",
    "summary": "对问题的总结",
    "description": "在[文件名]的[函数名]的[行号]行处，程序实现是[具体代码实现]，而文档[章节号]的需求是[需求描述]，实现与需求不一致，原因是[技术分析]。"
  }}, ...]
}}
```

**示例**:
- 如果没有问题:
```json
{{
  "review_process": "代码功能与需求文档完全一致，代码不存在缺陷。（输出你的思考过程，分多个步骤，说清楚你分析和推导的逻辑）",
  "issue": null
}}
```
- 如果有问题:
```json
{{
  "review_process": "在审查过程中发现，当输入为负数时，需求文档要求返回0，但代码实现返回了1，这与需求不符。（输出你的思考过程，分多个步骤，说清楚你分析和推导的逻辑）",
  "issue": [{{
    "level": "medium",
    "summary": "factorial函数对负数输入的处理与需求不一致",
    "description": "在 'algorithms/math_algorithms.cpp' 的 'factorial' 函数中，程序实现是 `if (n < 0) return 1;`，而math_algorithms.md文档第2章的需求是 `当n为负数时返回0`，实现与需求不一致，原因是代码错误地为负数输入返回了1。"
  }}, ...]
}}
```
**示例结束**

# 输入
## 需求片段
```
{requirement}
```

## 相关代码片段
```
{related_code}
```
/think
"""

GENERATE_PROMPT_TEMPLATE = """
# 任务说明
你是一位精通航天领域软件系统和C/C++编程的资深专家。请你根据以下代码和参考需求，生成描述该代码功能的需求片段，要求如下：

## 核心要求
1. **解释代码流程**: 详细分析代码的执行流程、逻辑结构和功能实现
2. **输出格式**: 使用Markdown格式输出

# 参考需求（用于格式和风格参考）
```
{reference_requirement}
```

# 相关代码
```
{related_code}
```

# 输出要求
请基于上述代码生成相应的需求描述，参考提供的需求格式和风格。直接输出生成的需求片段，使用Markdown格式。
/think
"""

RULE_EXTRACTION_PROMPT = """
你是一个代码审计专家。请分析以下编程规范文档的片段，提取其中的规则信息。
请提取：
1. "id": 规则编号（如 R-1-10-3）。
2. "description": 规则的文字描述。
3. "violation_code": "违背示例"下的代码片段。
4. "compliance_code": "遵循示例"下的代码片段。

文档内容：
{text}

请返回一个 JSON 列表，每个元素包含上述字段。
"""

ISSUE_EXTRACTION_PROMPT = """
你是一个测试专家。请分析以下软件问题单的内容，提取关键信息。
请提取：
1. "id": 问题单标识。
2. "desc": 问题描述的详细内容。
3. "opinion": 承制方处理意见或改正措施。
4. "trace_id": 问题追踪、用例标识或相关的函数名。

文档内容：
{text}

请返回一个 JSON 列表，每个元素包含上述字段。
"""